import 'package:cli_completion/cli_completion.dart';

const packageName = "bunny_cli";


class BunnyCliCommandRunner extends CompletionCommandRunner<int> {
  BunnyCliCommandRunner(super.executableName, super.description);

  
  
}